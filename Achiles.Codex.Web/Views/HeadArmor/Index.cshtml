@using Achilles.Codex.Web.Misc
@model IEnumerable<Achilles.Codex.Model.HeadArmor>

<h2>Head Armors</h2>
<p class="text-muted">
    BLA BLA
</p>
<hr />


@{
    ViewBag.Title = "Head Armor";
}

<h2>Index</h2>

<p>
    <div class="row">
        <div class="col-xs-2">
            <input type="text" class="form-control" id="filterAttributes" placeholder="fillter">
        </div>
        <div class="col-xs-2">
            @if (User.IsContributor()) //only contributors can add new attribute
            {<a class="btn btn-default" href="@Url.Action("Edit", "HeadArmor")" title="Add new head armor">
                <span class="glyphicon glyphicon-plus-sign"></span> New Head Armor
            </a>}
        </div>
    </div>
</p>